ru:
  time:
    human_format: "чч:мм"
    title: время
    pickadate_format: 'H:i'
    am: утра
    formats:
      default: ! '%a, %d %b %Y, %H:%M:%S %z'
      long: ! '%d %B %Y, %H:%M'
      short: ! '%d %b, %H:%M'
      med: ! '%-d %B, %H:%M'
      clock: ! '%H:%M'
      digits: ! '%Y%m%d_%H:%M'
  activerecord:
    errors: &errors
      format: ! '%{attribute} %{message}'
      messages:
        email: не корректный
      models:
        user:
          attributes:
            password_confirmation:
              confirmation: обязательно для заполнения
            password:
              blank: обязательно для заполнения
              too_short: 'не менее 6 символов'
            email:
              blank: обязательно для заполнения
  activemodel:
    errors:
      models:
        user_login_form:
          attributes:
            password:
              blank: обязательно для заполнения
              too_short: 'не менее 6 символов'
            email:
              blank: обязательно для заполнения
  http_erro:
    unauthorized: Неавторизован

